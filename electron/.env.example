# 이 파일을 .env.development / .env.production 으로 복사해서 값을 채워라.
# 두 파일 모두 .gitignore 대상 — 커밋되지 않는다.
#
# CI(GitHub Actions)에서는 repo Secrets/Variables 로 주입한다.
# - VITE_IMGBB_API_KEY : Secrets
# - VITE_MOHANI_BACKEND_URL : Variables (공개 URL이라 secret 아님)

VITE_MOHANI_BACKEND_URL=
VITE_IMGBB_API_KEY=
