# 프로덕션 빌드 — `vite build` 시 자동 로드.
# agent 패키지(prepublishOnly → build:ui → vite build)에서 별도 env 주입 없이 이 값이 박힌다.
# 다른 백엔드로 publish하고 싶으면 빌드 시 VITE_MOHANI_BACKEND_URL=... 로 override 가능.
VITE_MOHANI_BACKEND_URL=https://mohani.onrender.com
VITE_IMGBB_API_KEY=8eddd9274a6f55e9fd45ddcaff40dde1
